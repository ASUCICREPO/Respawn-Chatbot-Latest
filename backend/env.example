PORT=8000
# Allow the Next.js dev server to call the backend during local development.
CORS_ORIGIN=http://localhost:3000
# Bedrock settings (for deployed usage)
BEDROCK_KB_ID=
BEDROCK_MODEL_ID=
BEDROCK_MODEL_ARN=
